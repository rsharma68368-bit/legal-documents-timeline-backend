# Copy to .env and fill in values.
# Required for auth (RS256/ES256): your Supabase project URL (same as frontend NEXT_PUBLIC_SUPABASE_URL).
SUPABASE_URL=https://your-project-id.supabase.co
# Only needed for legacy HS256 projects; leave unset if using RS256/ES256.
# SUPABASE_JWT_SECRET=your-jwt-secret
MONGODB_URL=mongodb://localhost:27017
MONGODB_DATABASE=legal_timeline_db
UPLOAD_DIR=uploads
